const APP = {
  supabase: window.SUPABASE_CONFIG || {
    url: "https://niehzhjbwjmwpsyjxwyx.supabase.co",
    anonKey: "sb_publishable_G5WQEqcoTshJwCzJVSXcpw_JM5cTmAq"
  },
  reserved: new Set(["create", "login", "dashboard", "settings", "api", "admin", "assets", "index"]),
  demo: {
    username: "xsqezz", display_name: "xsqezz", bio: "Buduję rzeczy, które powinny istnieć. Kod, design i trochę chaosu po godzinach.",
    avatar_url: "https://xsqezz.github.io/assets/avatar.svg", banner_url: "", primary_color: "#c8f451", background_color: "#0b0f13", gradient: true,
    blur: 18, animations: true, font: "Manrope", layout: "stacked", badge: "founder", status: "online", views: 1842,
    links: { discord: "https://discord.com/", github: "https://github.com/xsqezz", tiktok: "https://www.tiktok.com/@xsqezz", youtube: "https://www.youtube.com/@xsqezz", instagram: "", custom: [{ label: "Sqezz Client", url: "https://github.com/xsqezz/Sqezz-Client" }] },
    music_url: "", autoplay: false, volume: .35
  },
  fonts: ["Manrope", "Space Grotesk", "DM Mono", "Georgia", "Verdana"]
};

let supabase = null;
let toastTimer;
const icons = { arrow: "↗", check: "✓", copy: "⧉", play: "▶", trash: "×" };
const $ = (selector, root = document) => root.querySelector(selector);
const $$ = (selector, root = document) => [...root.querySelectorAll(selector)];
const safeText = (value, fallback = "") => typeof value === "string" ? value : fallback;
const escapeHtml = (value) => safeText(value).replace(/&(?:amp|lt|gt|quot|#39);|[&<>"']/g, (character) => ({ "&amp;": "&", "&lt;": "<", "&gt;": ">", "&quot;": '"', "&#39;": "'", "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;", "'": "&#39;" })[character]);
const htmlSafeProfile = (value) => Array.isArray(value) ? value.map(htmlSafeProfile) : value && typeof value === "object" ? Object.fromEntries(Object.entries(value).map(([key, item]) => [key, htmlSafeProfile(item)])) : typeof value === "string" ? escapeHtml(value) : value;
const isUrl = (value) => { if (!value) return true; try { const url = new URL(value); return ["http:", "https:"].includes(url.protocol); } catch { return false; } };
const isUsername = (value) => /^[a-z0-9_-]{3,28}$/.test(value) && !APP.reserved.has(value);
const escapeSelector = (value) => CSS.escape ? CSS.escape(value) : value.replace(/[^a-zA-Z0-9_-]/g, "\\$&");
const showToast = (message, error = false) => { const toast = $("#toast"); toast.textContent = message; toast.className = `toast is-visible${error ? " error" : ""}`; clearTimeout(toastTimer); toastTimer = setTimeout(() => { toast.className = "toast"; }, 3200); };
const navigate = (path) => { history.pushState({}, "", path); renderRoute(); window.scrollTo({ top: 0, behavior: "smooth" }); };
const getPath = () => { const redirect = sessionStorage.getItem("gh-pages-path"); if (redirect) { sessionStorage.removeItem("gh-pages-path"); history.replaceState({}, "", redirect); } return location.pathname.replace(/^\/+|\/+$/g, "").toLowerCase(); };
const iconSvg = (name) => `<span aria-hidden="true">${name || icons.arrow}</span>`;

async function connectSupabase() { if (!APP.supabase.url || !APP.supabase.anonKey) return null; try { const module = await import("https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2/+esm"); supabase = module.createClient(APP.supabase.url, APP.supabase.anonKey); const hash = new URLSearchParams(location.hash.replace(/^#/, "")); const accessToken = hash.get("access_token"); const refreshToken = hash.get("refresh_token"); if (accessToken && refreshToken) { await supabase.auth.setSession({ access_token: accessToken, refresh_token: refreshToken }); history.replaceState({}, "", `${location.pathname}${location.search}`); } return supabase; } catch { return null; } }
async function currentUser() { if (!supabase) return null; try { const sessionResult = await Promise.race([supabase.auth.getSession(), new Promise((resolve) => setTimeout(() => resolve({ data: { session: null } }), 4000))]); return sessionResult?.data?.session?.user || null; } catch { return null; } }
async function requireSupabase() { if (!supabase) await connectSupabase(); return supabase; }

function renderHeader() { const userReady = Boolean(supabase); $("#site-header").innerHTML = `<a class="brand" href="/" data-nav><span class="brand-mark"><svg viewBox="0 0 24 24"><path d="m5 8 7-5 7 5v8l-7 5-7-5V8Zm4 3 3 2 3-2" /></svg></span>orbit</a><nav class="nav-actions"><a class="nav-link" href="/" data-nav>Explore</a><a class="nav-link" href="/create" data-nav>Create</a>${userReady ? `<a class="button primary" href="/dashboard" data-nav>Dashboard ${iconSvg(icons.arrow)}</a>` : `<a class="button ghost" href="/login" data-nav>Zaloguj się</a>`}</nav>`; $$("[data-nav]").forEach((link) => link.addEventListener("click", (event) => { event.preventDefault(); navigate(link.getAttribute("href")); })); }

function landingView() { return `<section class="hero"><div class="hero-copy"><p class="eyebrow">a better place for your internet self</p><h1>Twoje linki.<br /><span class="gradient-text">Twój orbit.</span></h1><p>Stwórz profil, który wygląda jak Ty. Zbieraj linki, dodaj muzykę, ustaw własne kolory i udostępnij jedną prostą stronę całemu światu.</p><div class="hero-actions"><a class="button primary" href="/create" data-nav>Stwórz profil ${iconSvg(icons.arrow)}</a><a class="button" href="#showcase">Zobacz przykłady</a></div><div class="hero-note"><span class="pulse-dot"></span> profiles with a point of view</div></div><div class="hero-showcase" aria-label="Podgląd przykładowego profilu"><span class="showcase-tag">live preview</span><div class="showcase-card primary-card">${miniProfile("nova", "Nova / digital artist", "Art, motion and little worlds made after midnight.", "#e1a8ff")}</div><div class="showcase-card secondary-card">${miniProfile("rune", "rune.dev", "building in public", "#6ce0ff")}</div></div></section><section class="feature-grid"><article class="feature-card"><span class="feature-number">01 / shape it</span><h3>Twój styl, bez szablonu</h3><p>Kolory, gradienty, blur, fonty i układ. Każdy profil może mieć własny rytm.</p></article><article class="feature-card"><span class="feature-number">02 / own it</span><h3>Jedna strona, cała historia</h3><p>Linki, social media, muzyka i projekty w miejscu, które możesz naprawdę nazwać swoim.</p></article><article class="feature-card"><span class="feature-number">03 / share it</span><h3>Gotowe do wysłania</h3><p>Twój profil mieszka pod prostym adresem. Skopiuj link i wrzuć go tam, gdzie jesteś.</p></article></section><section id="showcase"><div class="section-heading"><h2>Małe strony.<br /><span class="gradient-text">Duża energia.</span></h2><p>Orbit jest dla twórców, budowniczych, artystów i każdego, kto chce mieć własny kawałek internetu.</p></div><div class="gallery">${miniGallery()}</div></section><footer class="app-footer"><span>orbit / profile platform</span><span>made for the internet's next chapter</span></footer>`; }
function miniProfile(user, name, bio, color) { return `<div class="mini-banner" style="background:linear-gradient(120deg,${color},#151b25 62%,#c8f451)"></div><div class="mini-profile"><div class="mini-avatar" style="background:linear-gradient(135deg,${color},#c8f451)"></div><div><p class="mini-name">${name}</p><p class="mini-handle">@${user}</p></div></div><p class="mini-bio">${bio}</p><div class="mini-links"><span class="mini-link"></span><span class="mini-link"></span><span class="mini-link"></span></div>`; }
function miniGallery() { return [miniProfile("mila", "mila / visual notes", "images, textures & quiet things", "#ffae83"), miniProfile("kyo", "kyo.exe", "games · code · late nights", "#b0f164"), miniProfile("sol", "sol studio", "designing a softer web", "#d29bff")].map((content) => `<article class="profile-card">${content}</article>`).join(""); }

function loginView() { return `<div class="auth-layout"><section class="auth-card"><p class="eyebrow">your orbit awaits</p><h1>Wejdź.<br /><span class="gradient-text">Zacznij tworzyć.</span></h1><p class="muted small">Zaloguj się, aby zapisać profil, edytować go i sprawdzić, ile osób go odwiedziło.</p><div class="form-stack" style="margin-top:24px"><button class="button primary" id="github-login">Kontynuuj przez GitHub ${iconSvg(icons.arrow)}</button><div class="section-title"><span>albo email</span></div><div class="field"><label class="label" for="email-input">Email</label><input class="input" id="email-input" type="email" placeholder="you@example.com" autocomplete="email" /></div><button class="button" id="email-login">Wyślij magiczny link</button><p class="field-hint">Nie masz jeszcze konta? To właśnie je utworzy.</p></div></section><aside class="panel">${miniProfile("you", "your next profile", "a little space for everything you are", "#6ce0ff")}</aside></div>`; }

function createDefaults(profile = {}) { return { ...APP.demo, ...profile, links: { ...APP.demo.links, ...(profile.links || {}), custom: Array.isArray(profile.links?.custom) ? profile.links.custom : APP.demo.links.custom } }; }
function editorView(profile = createDefaults()) { const p = htmlSafeProfile(createDefaults(profile)); return `<div class="editor-layout"><section class="panel"><div class="panel-heading"><div><p class="eyebrow">profile studio</p><h2>Ustaw swój<br /><span class="gradient-text">kawałek sieci.</span></h2><p>Zmiany zapisują się dopiero po kliknięciu „Zapisz profil”. Podgląd aktualizuje się od razu.</p></div><span class="mono muted small">/ create</span></div><form id="profile-form" class="form-stack"><div class="section-block"><div class="section-title"><span>Tożsamość</span><span class="mono muted">01</span></div><div class="field-grid"><div class="field"><label class="label" for="username">Username *</label><input class="input" id="username" name="username" value="${safeText(p.username)}" placeholder="twoja_nazwa" maxlength="28" required /><span class="field-hint">3-28 znaków: małe litery, cyfry, - lub _</span></div><div class="field"><label class="label" for="display_name">Display name</label><input class="input" id="display_name" name="display_name" value="${safeText(p.display_name)}" maxlength="40" /></div></div><div class="field"><label class="label" for="bio">Opis</label><textarea class="textarea" id="bio" name="bio" maxlength="180" placeholder="Napisz kilka słów o sobie...">${safeText(p.bio)}</textarea><span class="field-hint">Maksymalnie 180 znaków.</span></div><div class="field-grid"><div class="field"><label class="label" for="avatar_url">Avatar URL</label><input class="input" id="avatar_url" name="avatar_url" value="${safeText(p.avatar_url)}" type="url" placeholder="https://..." /></div><div class="field"><label class="label" for="banner_url">Banner URL</label><input class="input" id="banner_url" name="banner_url" value="${safeText(p.banner_url)}" type="url" placeholder="https://..." /></div></div></div><div class="section-block"><div class="section-title"><span>Linki społecznościowe</span><span class="mono muted">02</span></div><div class="social-grid">${socialField("github", p.links.github, "GH")} ${socialField("discord", p.links.discord, "DS")} ${socialField("tiktok", p.links.tiktok, "TK")} ${socialField("youtube", p.links.youtube, "YT")} ${socialField("instagram", p.links.instagram, "IG")}</div><div class="section-title" style="margin-top:18px"><span>Własne linki</span><button class="button ghost" type="button" id="add-custom-link">+ Dodaj</button></div><div id="custom-links">${p.links.custom.map(customLinkField).join("")}</div></div><div class="section-block"><div class="section-title"><span>Wygląd</span><span class="mono muted">03</span></div><div class="field-grid"><div class="field"><label class="label" for="primary_color">Kolor główny</label><input class="input" id="primary_color" name="primary_color" value="${safeText(p.primary_color)}" type="color" /></div><div class="field"><label class="label" for="background_color">Kolor tła</label><input class="input" id="background_color" name="background_color" value="${safeText(p.background_color)}" type="color" /></div></div><div class="field-grid"><label class="check-row"><input id="gradient" name="gradient" type="checkbox" ${p.gradient ? "checked" : ""} /> Gradient tła</label><label class="check-row"><input id="animations" name="animations" type="checkbox" ${p.animations ? "checked" : ""} /> Animacje wejścia</label></div><div class="field"><label class="label" for="blur">Blur szkła</label><div class="range-row"><input class="range" id="blur" name="blur" type="range" min="0" max="32" value="${Number(p.blur) || 18}" /><span class="range-value" id="blur-value">${Number(p.blur) || 18}px</span></div></div><div class="field-grid"><div class="field"><label class="label" for="font">Font</label><select class="select" id="font" name="font">${APP.fonts.map((font) => `<option ${font === p.font ? "selected" : ""}>${font}</option>`).join("")}</select></div><div class="field"><label class="label" for="layout">Układ</label><select class="select" id="layout" name="layout"><option value="stacked" ${p.layout === "stacked" ? "selected" : ""}>Stacked</option><option value="compact" ${p.layout === "compact" ? "selected" : ""}>Compact</option></select></div></div></div><div class="section-block"><div class="section-title"><span>Muzyka i status</span><span class="mono muted">04</span></div><div class="field"><label class="label" for="music_url">Music URL</label><input class="input" id="music_url" name="music_url" value="${safeText(p.music_url)}" type="url" placeholder="https://.../track.mp3" /></div><div class="field-grid"><label class="check-row"><input id="autoplay" name="autoplay" type="checkbox" ${p.autoplay ? "checked" : ""} /> Autoplay</label><div class="field"><label class="label" for="status">Status</label><select class="select" id="status" name="status"><option value="online" ${p.status === "online" ? "selected" : ""}>Online</option><option value="offline" ${p.status === "offline" ? "selected" : ""}>Offline</option></select></div></div><div class="field"><label class="label" for="badge">Badge</label><input class="input" id="badge" name="badge" value="${safeText(p.badge)}" maxlength="24" placeholder="founder" /></div><div class="field"><label class="label" for="volume">Głośność</label><div class="range-row"><input class="range" id="volume" name="volume" type="range" min="0" max="1" step=".05" value="${Number(p.volume) || .35}" /><span class="range-value" id="volume-value">${Math.round((Number(p.volume) || .35) * 100)}%</span></div></div></div><div class="editor-actions"><button class="button primary" type="submit">Zapisz profil ${iconSvg(icons.check)}</button><button class="button" type="button" id="open-login">Zaloguj się</button></div></form></section><aside class="preview-wrap"><p class="preview-label">live preview / <span id="preview-state">draft</span></p><div class="preview-stage"><div id="profile-preview" class="preview-profile"></div></div></aside></div>`; }
function socialField(name, value, icon) { return `<div class="social-input"><span class="social-icon">${icon}</span><input class="input" data-social="${name}" type="url" value="${escapeHtml(value)}" placeholder="${name}.com/..." aria-label="${name}" /></div>`; }
function customLinkField(item = {}) { return `<div class="add-link custom-link-row"><input class="input" data-custom-label value="${escapeHtml(item.label)}" maxlength="36" placeholder="Nazwa linku" /><input class="input" data-custom-url type="url" value="${escapeHtml(item.url)}" placeholder="https://..." /><button class="remove-button" type="button" aria-label="Usuń link">${icons.trash}</button></div>`; }

function dashboardView(user, profile) { if (!user) return `<div class="auth-card"><p class="eyebrow">private area</p><h1>Najpierw się<br /><span class="gradient-text">zaloguj.</span></h1><p class="muted small">Dashboard pokazuje tylko profile należące do zalogowanego konta.</p><a class="button primary" href="/login" data-nav>Zaloguj się ${iconSvg(icons.arrow)}</a></div>`; if (!profile) return `<div class="empty-state"><p class="eyebrow">dashboard</p><h2>Twój orbit jest pusty.</h2><p>Stwórz pierwszy profil i zacznij zbierać swoje linki w jednym miejscu.</p><a class="button primary" href="/create" data-nav>Stwórz profil</a></div>`; return `<div class="dashboard-layout"><div class="dashboard-hero"><div><p class="eyebrow">welcome back</p><h1>Twój <span class="gradient-text">dashboard.</span></h1></div><button class="button ghost" id="logout">Wyloguj się</button></div><div class="dashboard-grid"><div class="metric"><span class="metric-label">Wyświetlenia</span><strong class="metric-value">${Number(profile.views || 0).toLocaleString("pl-PL")}</strong></div><div class="metric"><span class="metric-label">Username</span><strong class="metric-value">@${safeText(profile.username)}</strong></div><div class="metric"><span class="metric-label">Utworzono</span><strong class="metric-value">${new Date(profile.created_at || Date.now()).toLocaleDateString("pl-PL")}</strong></div></div><div class="profile-manage"><div><strong>${safeText(profile.display_name || profile.username)}</strong><span>https://xsqezz.github.io/${safeText(profile.username)}</span></div><div class="manage-actions"><button class="button" id="copy-profile">${icons.copy} Kopiuj link</button><a class="button" href="/${safeText(profile.username)}" data-nav>Podgląd ${iconSvg(icons.arrow)}</a><a class="button primary" href="/create" data-nav>Edytuj profil</a><button class="button danger" id="delete-profile">Usuń profil</button></div></div></div>`; }

function profileView(profile) { const p = createDefaults(profile); const root = document.createElement("article"); root.className = `public-profile ${p.animations ? "has-animations" : ""}`; root.style.setProperty("--profile-accent", safeColor(p.primary_color, APP.demo.primary_color)); root.style.setProperty("--profile-bg", safeColor(p.background_color, APP.demo.background_color)); root.style.setProperty("--profile-blur", `${Math.min(Math.max(Number(p.blur) || 0, 0), 32)}px`); root.style.fontFamily = fontStack(p.font); root.innerHTML = `<div class="public-card"><div class="public-banner"></div><div class="public-content"><div class="public-avatar"></div><div class="public-status ${p.status === "offline" ? "offline" : ""}"><i></i>${p.status === "offline" ? "offline" : "online"}</div><p class="public-username">@${safeText(p.username)}</p><h1 class="public-name"></h1><p class="public-bio"></p><span class="public-badge" ${p.badge ? "" : "hidden"}></span><div class="public-links"></div><div class="public-music" ${p.music_url ? "" : "hidden"}><button class="music-toggle" type="button" aria-label="Odtwórz muzykę">${icons.play}</button><span>now playing</span><input class="music-volume" type="range" min="0" max="1" step=".05" value="${Number(p.volume) || .35}" /><audio preload="none"></audio></div><footer class="public-footer"><span>${Number(p.views || 0).toLocaleString("pl-PL")} visits</span><span>orbit / ${new Date().getFullYear()}</span></footer></div></div>`; const banner = $(".public-banner", root); if (isUrl(p.banner_url)) banner.style.backgroundImage = `url("${p.banner_url}")`; else banner.style.background = `linear-gradient(120deg, ${safeColor(p.primary_color, APP.demo.primary_color)}, #151c27 60%, #0d1116)`; const avatar = $(".public-avatar", root); if (isUrl(p.avatar_url)) avatar.style.backgroundImage = `url("${p.avatar_url}")`; const name = $(".public-name", root); name.textContent = safeText(p.display_name || p.username); $(".public-bio", root).textContent = safeText(p.bio); const badge = $(".public-badge", root); badge.textContent = safeText(p.badge); if (!p.badge) badge.hidden = true; const linkWrap = $(".public-links", root); const links = [...Object.entries(p.links || {}).filter(([key, value]) => key !== "custom" && value && isUrl(value)).map(([key, value]) => ({ label: key[0].toUpperCase() + key.slice(1), url: value })), ...(p.links?.custom || []).filter((link) => link?.label && link.url && isUrl(link.url))]; links.forEach((link) => { const anchor = document.createElement("a"); anchor.className = "public-link"; anchor.href = link.url; anchor.target = "_blank"; anchor.rel = "noopener noreferrer"; anchor.textContent = link.label; anchor.insertAdjacentHTML("beforeend", `<span aria-hidden="true">${icons.arrow}</span>`); linkWrap.append(anchor); }); setupMusic(root, p); return root; }
function safeColor(value, fallback) { return /^#[0-9a-f]{6}$/i.test(value || "") ? value : fallback; }
function fontStack(font) { const stacks = { Manrope: "Manrope, sans-serif", "Space Grotesk": "'Space Grotesk', sans-serif", "DM Mono": "'DM Mono', monospace", Georgia: "Georgia, serif", Verdana: "Verdana, sans-serif" }; return stacks[font] || stacks.Manrope; }
function collectForm() { const form = $("#profile-form"); const data = Object.fromEntries(new FormData(form).entries()); const links = { custom: $$(".custom-link-row").map((row) => ({ label: $("[data-custom-label]", row).value.trim(), url: $("[data-custom-url]", row).value.trim() })).filter((item) => item.label || item.url) }; ["discord", "github", "tiktok", "youtube", "instagram"].forEach((name) => { links[name] = $(`[data-social="${name}"]`).value.trim(); }); return { username: data.username.trim().toLowerCase(), display_name: data.display_name.trim(), bio: data.bio.trim(), avatar_url: data.avatar_url.trim(), banner_url: data.banner_url.trim(), primary_color: data.primary_color, background_color: data.background_color, gradient: $("#gradient").checked, blur: Number(data.blur), animations: $("#animations").checked, font: data.font, layout: data.layout, badge: data.badge.trim(), status: data.status, music_url: data.music_url.trim(), autoplay: $("#autoplay").checked, volume: Number(data.volume), links }; }
function validateProfile(profile) { const errors = []; if (!isUsername(profile.username)) errors.push("Username ma nieprawidłowy format albo jest zarezerwowany."); if (profile.bio.length > 180) errors.push("Opis może mieć maksymalnie 180 znaków."); if (![profile.avatar_url, profile.banner_url, profile.music_url, ...Object.values(profile.links).filter((value) => typeof value === "string"), ...(profile.links.custom || []).map((item) => item.url)].every(isUrl)) errors.push("Każdy adres URL musi zaczynać się od http:// lub https://."); if (!/^#[0-9a-f]{6}$/i.test(profile.primary_color) || !/^#[0-9a-f]{6}$/i.test(profile.background_color)) errors.push("Kolory mają nieprawidłowy format."); return errors; }
function updatePreview(profile) { const stage = $("#profile-preview"); if (!stage) return; stage.replaceWith(profileView({ ...profile, views: 0 })); const next = $(".public-card"); next.parentElement.id = "profile-preview"; const state = $("#preview-state"); if (state) state.textContent = "live"; }
function setupEditor(profile) { const form = $("#profile-form"); if (!form) return; const refresh = () => { try { updatePreview(collectForm()); } catch { /* keep typing uninterrupted */ } }; $$("input, textarea, select", form).forEach((input) => input.addEventListener("input", refresh)); $("#blur").addEventListener("input", (event) => { $("#blur-value").textContent = `${event.target.value}px`; }); $("#volume").addEventListener("input", (event) => { $("#volume-value").textContent = `${Math.round(Number(event.target.value) * 100)}%`; }); $("#add-custom-link").addEventListener("click", () => { $("#custom-links").insertAdjacentHTML("beforeend", customLinkField()); bindRemoveButtons(); }); bindRemoveButtons(); $("#open-login").addEventListener("click", () => navigate("/login")); form.addEventListener("submit", async (event) => { event.preventDefault(); const profileData = collectForm(); const errors = validateProfile(profileData); if (errors.length) return showToast(errors[0], true); const client = await requireSupabase(); const user = await currentUser(); if (!client || !user) { localStorage.setItem("orbit-draft", JSON.stringify(profileData)); showToast("Draft zapisany lokalnie. Zaloguj się i skonfiguruj Supabase, aby opublikować profil.", true); return; } const { data, error } = await client.from("profiles").upsert({ ...profileData, user_id: user.id }, { onConflict: "username" }).select().single(); if (error) return showToast(error.code === "23505" ? "Ta nazwa użytkownika jest już zajęta." : error.message, true); showToast("Profil zapisany."); navigate(`/${data.username}`); }); refresh(); }
function bindRemoveButtons() { $$(".remove-button").forEach((button) => { if (button.dataset.bound) return; button.dataset.bound = "true"; button.addEventListener("click", () => button.closest(".custom-link-row").remove()); }); }
function setupMusic(root, profile) { const music = $(".public-music", root); if (!profile.music_url || !music) return; const audio = $("audio", music); const toggle = $(".music-toggle", music); const volume = $(".music-volume", music); audio.src = profile.music_url; audio.volume = Number(profile.volume) || .35; toggle.addEventListener("click", async () => { try { if (audio.paused) { await audio.play(); toggle.textContent = "Ⅱ"; } else { audio.pause(); toggle.textContent = icons.play; } } catch { showToast("Nie udało się odtworzyć muzyki.", true); } }); volume.addEventListener("input", () => { audio.volume = Number(volume.value); }); if (profile.autoplay) audio.play().catch(() => {}); }

async function getProfile(username) { const client = await requireSupabase(); if (!client) return username === APP.demo.username ? APP.demo : null; const { data, error } = await client.from("profiles").select("*").eq("username", username).maybeSingle(); if (error || !data) return username === APP.demo.username ? APP.demo : null; client.rpc("increment_profile_views", { profile_username: username }).then(() => {}); return data; }
async function getOwnedProfile(user) { if (!supabase || !user) return null; return (await supabase.from("profiles").select("*").eq("user_id", user.id).maybeSingle()).data; }
async function renderRoute() { const path = getPath(); renderHeader(); const app = $("#app"); app.innerHTML = `<div class="loading"><span><i class="spinner"></i> loading orbit</span></div>`; let content; if (!path) content = landingView(); else if (path === "login") content = loginView(); else if (path === "create") { const user = await currentUser(); let profile = user ? await getOwnedProfile(user) : null; const draft = localStorage.getItem("orbit-draft"); if (!profile && draft) { try { profile = JSON.parse(draft); } catch {} } content = editorView(profile || APP.demo); } else if (path === "dashboard") { const user = await currentUser(); content = dashboardView(user, user ? await getOwnedProfile(user) : null); } else if (isUsername(path)) { const profile = await getProfile(path); if (!profile) content = `<div class="not-found"><div><p class="eyebrow">404 / no orbit here</p><h1>Ten profil<br /><span class="gradient-text">nie istnieje.</span></h1><p>Sprawdź username albo wróć do odkrywania.</p><a class="button primary" href="/" data-nav>Wróć na start</a></div></div>`; else { app.replaceChildren(profileView(profile)); return; } } else content = `<div class="not-found"><div><p class="eyebrow">404 / lost in space</p><h1>Nie znaleziono<br /><span class="gradient-text">tej strony.</span></h1><a class="button primary" href="/" data-nav>Wróć na start</a></div></div>`; app.innerHTML = content; $$("[data-nav]", app).forEach((link) => link.addEventListener("click", (event) => { event.preventDefault(); navigate(link.getAttribute("href")); })); if (path === "login") setupLogin(); if (path === "create") setupEditor(); if (path === "dashboard") setupDashboard(); }
function setupLogin() { $("#github-login")?.addEventListener("click", async () => { const client = await requireSupabase(); if (!client) return showToast("Dodaj config.js z danymi Supabase.", true); await client.auth.signInWithOAuth({ provider: "github", options: { redirectTo: `${location.origin}/dashboard` } }); }); $("#email-login")?.addEventListener("click", async () => { const email = $("#email-input").value.trim(); if (!email) return showToast("Podaj adres email.", true); const client = await requireSupabase(); if (!client) return showToast("Dodaj config.js z danymi Supabase.", true); const { error } = await client.auth.signInWithOtp({ email, options: { emailRedirectTo: `${location.origin}/dashboard` } }); if (error) showToast(error.message, true); else showToast("Sprawdź skrzynkę email."); }); }
function setupDashboard() { $("#logout")?.addEventListener("click", async () => { await supabase?.auth.signOut(); renderHeader(); navigate("/"); }); $("#copy-profile")?.addEventListener("click", async () => { const text = $(".profile-manage span").textContent; await navigator.clipboard?.writeText(text); showToast("Link skopiowany."); }); $("#delete-profile")?.addEventListener("click", async () => { if (!confirm("Usunąć profil bez możliwości cofnięcia?")) return; const user = await currentUser(); if (!user) return; const { error } = await supabase.from("profiles").delete().eq("user_id", user.id); if (error) showToast(error.message, true); else { showToast("Profil usunięty."); navigate("/dashboard"); } }); }

function setupLogin() { $("#email-login")?.addEventListener("click", async () => { const email = $("#email-input").value.trim(); if (!email) return showToast("Podaj adres email.", true); const client = await requireSupabase(); if (!client) return showToast("Brak konfiguracji Supabase.", true); const { error } = await client.auth.signInWithOtp({ email, options: { emailRedirectTo: `${location.origin}/dashboard` } }); if (error) { const message = /rate limit/i.test(error.message || "") ? "Limit maili został chwilowo przekroczony. Użyj ostatniego linku z emaila albo spróbuj ponownie później." : error.message; showToast(message, true); } else showToast("Sprawdz skrzynke email."); }); }
function loginView() { return `<div class="auth-layout"><section class="auth-card"><p class="eyebrow">your orbit awaits</p><h1>Wejdz.<br /><span class="gradient-text">Zacznij tworzyc.</span></h1><p class="muted small">Zaloguj sie, aby zapisac profil, edytowac go i sprawdzic, ile osob go odwiedzilo.</p><div class="form-stack" style="margin-top:24px"><div class="field"><label class="label" for="email-input">Email</label><input class="input" id="email-input" type="email" placeholder="you@example.com" autocomplete="email" /></div><button class="button primary" id="email-login">Wyslij magiczny link</button><p class="field-hint">Nie masz jeszcze konta? To wlasnie je utworzy.</p></div></section><aside class="panel">${miniProfile("you", "your next profile", "a little space for everything you are", "#6ce0ff")}</aside></div>`; }
window.addEventListener("popstate", renderRoute); window.addEventListener("DOMContentLoaded", () => { renderRoute(); connectSupabase().then((client) => { if (!client) return; client.auth.onAuthStateChange((event) => { if (["SIGNED_IN", "SIGNED_OUT", "TOKEN_REFRESHED"].includes(event)) setTimeout(renderRoute, 0); }); renderRoute(); }).catch(() => {}); });
